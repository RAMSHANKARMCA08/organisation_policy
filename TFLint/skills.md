# tflint skills

Use tflint for Terraform quality, provider checks, deprecated constructs, required versions/providers, and documentation. Initialize pinned plugins through an approved channel and run before terrascan and opa.




Security toolchain: Gitleaks scans secrets and Checkov scans infrastructure-as-code. Both use pinned CI versions, normalized findings, and the shared exception/release-gate process.

