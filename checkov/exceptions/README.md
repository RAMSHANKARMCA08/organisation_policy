# Checkov exceptions

Use repository-scoped YAML exception records validated by `policy/exception.schema.json`. Exceptions must include the Checkov check ID, business reason, approver, ticket, and expiry.
