# Repository inventory skills

Use this folder for one metadata file per organization repository. Store only repository name, project, and two points of contact. Never store tokens, passwords, or connection strings.




Security toolchain: Gitleaks scans secrets and Checkov scans infrastructure-as-code. Both use pinned CI versions, normalized findings, and the shared exception/release-gate process.

