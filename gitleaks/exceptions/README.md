# Gitleaks exceptions

Store repository-scoped YAML exceptions here. Each exception must pass `policy/exception.schema.json`, identify the exact policy ID, include approval and a ticket reference, and have a future `valid_till` date.
